import { Component } from "react";

class Reaction extends Component {
  render() {
    console.log(1);
    console.log(2);
    const count = 0;
    return (
      <div>
        <h1> {count + 1}</h1>
      </div>
    );
  }
}

export default Reaction;
